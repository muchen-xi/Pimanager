# 贡献指南

感谢你对 PiManager v2 的关注！以下是参与贡献的方式。

## 报告 Bug

1. 确认问题可以在最新版本中重现
2. 使用以下模板提交 issue：

```
**问题描述**
[清晰描述发生了什么]

**复现步骤**
1. 打开应用...
2. 点击...
3. 观察...

**预期行为**
[你期望发生什么]

**环境信息**
- 操作系统: Windows / Linux
- Python 版本: 3.x
- PiManager 版本: v2.x.x
```

## 功能建议

欢迎通过 issue 提出新功能建议。请描述：
- 你希望解决的问题
- 建议的解决方案
- 可选的替代方案

## 代码风格

本项目遵循 [风格规范](_style_ref/STYLE_GUIDE.md)（仅供内部参考）。核心要点：

| 规则 | 说明 |
|------|------|
| 命名 | 变量/函数 `snake_case`，类 `PascalCase`，常量 `UPPER_SNAKE_CASE` |
| 缩进 | 4 空格，无 Tab |
| 引号 | 中文字符串用 `"`，ASCII 用 `'` |
| 导入顺序 | stdlib → 第三方 → 本地模块 |
| 类型标注 | 不使用 |
| 日志 | `print()` 不使用 `logging` |
| 异常 | 特定异常优先，错误返回 `{"error": msg}` 字典 |

## Pull Request 流程

1. Fork 本仓库
2. 创建功能分支：`git checkout -b feature/你的功能`
3. 确保代码符合风格规范
4. 运行测试：`python -m tests.run_all`
5. 提交前确保所有测试通过
6. 提交 PR，描述变更内容和原因

## 许可证

本项目使用 MIT License。贡献即表示你同意你的代码在 MIT 许可下发布。
